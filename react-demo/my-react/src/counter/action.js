export const INCREMENT='INCREMENT';
export const DECREMENT='DECREMENT';

export const incrementClick = (state)=>({
    type:INCREMENT,
    state

});
export const decrementClick = (state)=>({
    type:INCREMENT,
    state

});